# fastcodon — FastAPI, natively in Codon.
# See ../roadmap.md for the dependency graph and bottom-up build order.
# This package is built leaves-first: net (sockets+selector) -> crypto -> tls -> reactor -> ...
